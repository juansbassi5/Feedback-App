generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Review {
  id          String   @id @default(cuid())
  rating      Int
  comment     String
  driverId    String
  passengerId String
  createdAt   DateTime @default(now())
}